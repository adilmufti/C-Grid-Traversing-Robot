# C-Grid-Traversing-Robot
This is a robot that traverses a grid, which is full of red obstacle blocks, until it finds a green marker. The robot does not store the location of anything, it follows the same algorithm everytime is passes through the grid, in order to find the green marker.
